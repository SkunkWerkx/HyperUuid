# HyperUuid
High-performance, allocation-free RFC 9562 UUID generation. One Rust core, direct native FFI into C#, Kotlin, and Python — no runtime bridge, no reflection. Runs on the server and all the way out to the browser.
